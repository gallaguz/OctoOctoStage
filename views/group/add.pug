extends ../layout

block content
    div
        h1 Add group
        p
            form(action="/groups/add" method="POST")
                label(for="new_group") New group
                input(type="text" id="new_group" value="" name="name")
                input(type="submit" value="Add group" )
